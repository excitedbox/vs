export default class TypeScriptExport {
    public name: string;
    public isDefault: boolean;

    constructor(name, isDefault) {
        this.name = name;
        this.isDefault = isDefault;
    }
}